# Par défaut on déploie tout sur la machine locale qui possède Docker.
# Pour déployer sur une machine distante, remplacer ansible_connection=local
# par ansible_host=<IP> et ansible_user=<user> (accès SSH + docker group).

[docker_host]
localhost ansible_connection=local
